export function hideLoading() {
    document.getElementById("loading").style.display = "none";
  }

// Haalt de id op uit de HTML en voegt er een display: none aan toe, zodat hij onzichtbaar wordt.